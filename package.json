{
  "name": "liquity",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile:contracts": "yarn workspace @liquity/contracts compile",
    "build:lib": "yarn workspace @liquity/lib build",
    "bootstrap": "yarn install && yarn compile:contracts && yarn build:lib"
  }
}
